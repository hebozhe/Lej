def fun sumEven as:
    take list[nat] nums;
    expect nat;
    def nat sum as 0;
    def nat numsLen as len[nums];
    def nat i as 0;
    do this numsLen times:
        if nums[i] % 2 = 0:
            sum as sum + nums[i];
            \
        i as i + 1;
        \
    give sum;
